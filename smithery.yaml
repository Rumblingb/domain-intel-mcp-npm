startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      proKey:
        type: string
        description: "Pro key for unlimited usage"
        default: ""
  commandFunction: |
    (config) => ({command: 'node', args: ['index.js', '--pro-key', config.proKey || '']})
